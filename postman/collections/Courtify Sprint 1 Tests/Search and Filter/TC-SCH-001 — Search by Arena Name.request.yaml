$kind: http-request
name: 'TC-SCH-001 — Search by Arena Name'
method: GET
url: '{{base_url}}/arenas'
order: 1000
description: 'TC-SCH-001: Frontend search test — verifies the API returns results when searching by arena name keyword.'
queryParams:
  - key: search
    value: Sports
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("TC-SCH-001: Status is 200", () => pm.response.to.have.status(200));
      pm.test("TC-SCH-001: Response is an array", () => {
          const json = pm.response.json();
          pm.expect(json).to.be.an("array");
      });
